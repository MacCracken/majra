include "lib/syscalls.cyr"
include "lib/alloc.cyr"
include "lib/freelist.cyr"
include "lib/string.cyr"
include "lib/str.cyr"
include "lib/fmt.cyr"
include "lib/fnptr.cyr"

fn inner() { ret2(999, 888); }
fn probe(x) { ret2(x * 10, x * 100); }
fn intervening(y) { var z = inner(); return y + 1; }

fn main() {
    alloc_init();
    var a = probe(5);
    var junk = intervening(7);
    var b = rethi();
    syscall(1,1,"hi=",3); fmt_int(a); syscall(1,1," lo_after_ret2fn=",17); fmt_int(b); syscall(1,1,"\n",1);
    return 0;
}
