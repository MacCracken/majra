---
name: Majra Documentation Health
description: Living ledger of doc currency in the majra repo — fresh / stale / read-through / evergreen / frozen, refreshed as docs are touched
type: state
---

# Documentation Health — majra

> **Last refresh**: 2026-09-21 (2.9.1 — toolchain patch, cyrius 6.6.4 → 6.6.6; no `src/` change beyond four comment blocks the pin made untrue. Doc-relevant on three axes. **(1) The pin moved and the folds did not**: sigil / patra / sandhi / sakshi / bayan are byte-identical to 6.6.4's snapshot and each is its repo's latest tag, so every "X under 6.6.4" in the tree became "X under 6.6.6, unchanged" rather than a new number — `state.md`, `dependency-watch.md`, `threat-model.md`, `README.md`, `cyrius-quirks.md`, `testing.md` re-anchored; the snapshot is 111 (the cx-target `alloc_cx.cyr`), the lock 111 + `6.6.6` trailer, bare-`lib sync` 53. **(2) A question this tree had written down got its answer**: `dependency-watch.md`'s `ESYSXLAT` bullet said *check whether the row count moved on the next bump* — it did, 44 → 60, and two of the sixteen new rows are `nanosleep` 35 and `sendto` 44, the numbers 2.7.3's account turns on. majra's code is unchanged and correct under both toolchains, but four `src/` comments asserted 44 was not a row; re-tensed (the only `src/` diff, comment-only in every bundle), quirk §9 re-derived with the gate's own pipeline, §2 restated (the fixup table is an *initial* capacity, growable since 6.2.0 — three docs had called it a cap). **(3) A blocker closed upstream without anyone asking**: 6.6.6's `lib/io.cyr` includes `lib/args_agnos.cyr`, which is the `_agnos_getenv` gap `state.md` and the roadmap had carried since 2.7.3 — verified with a clean-room probe per profile (zero undefined, x86_64 and `--agnos`), the state row struck through, the roadmap's "Waiting on upstream" emptied with a note and the majra-side half (the backends / patra-queue *suites* still use Linux arities) re-homed to the 2.9 line with a trigger. The roadmap's pre-written "Moving the cyrius pin to 6.6.6" section shipped and was deleted, its measured claims re-checked first. Two rows this sweep found stale on their own: `state.md`'s signed / backends sidecar counts had read 19 / 23 since 2.8.1 while the files hold 20 / 25, and `testing.md`'s suite table still said 637 (2.7.3) through three releases that took it to 1,466 — both re-anchored. `CHANGELOG.md` gained a Known-issues block carrying the `uname_release` duplicate forward (re-checked, unchanged) and a second sigil warning line 6.6.5 promoted from a `note:` (same static storage, one more line). Prior entry:) | > **Last refresh**: 2026-09-15 (2.9.0 — two wire breaks; new `docs/guides/migration-2.9.0.md`; semver.md gains the wire-break exception; CHANGELOG / VERSION / state.md / roadmap.md (2.9.0 shipped and removed) / threat-model.md / README.md touched). Before that 2026-09-15 (2.8.2 — heartbeat owned keys, relay tombstone compaction, fuzz oracles; CHANGELOG / VERSION / state.md / roadmap.md (Next patch shipped and removed) / testing.md touched). Before that 2026-09-15 (2.8.1 — P(-1) sweep: new `docs/audit/2026-09-15-audit.md`; CHANGELOG / VERSION / state.md / roadmap.md (2.9.0 + Next patch) / threat-model.md (salt, replay, DNS rebinding, anchored verify, parked publishers) / cyrius-quirks.md §9 / dependency-watch.md touched). Before that 2026-09-15 (2.8.0 — namespace minor: `MAJRA_ERR_*` with deprecated bare aliases, `majra_ws_*` collision rename, hoosh ratelimit issue deleted; CHANGELOG / VERSION / README / overview.md / state.md / roadmap.md / semver.md touched). Before that 2026-09-14 (2.7.3 — toolchain patch, cyrius 6.6.2 → 6.6.4, plus an aarch64 filing in the class cyrius 6.6.4 swept from its own stdlib (its pin-sweep list did not name majra — nothing had cross-built majra until daimon did) and a barrier defect the aarch64 run surfaced. Doc-relevant on four axes. **The first consumer issue filed from a cross-build of a shipped bundle** — `docs/development/issues/archived/2026-09-14-raw-x86-syscall-numbers-aarch64.md`, from daimon's `cyrius build --aarch64` of the vendored `dist/majra.cyr` (the archived pq-dequeue issue was consumer-filed too, from agnosai's benchmark): three raw x86_64 syscall numbers, two of which ran as *different, valid* syscalls on aarch64-Linux and one of which was `-ENOSYS` (per-site table in CHANGELOG.md § [2.7.3]) — and only the `-ENOSYS` one ever produced a build warning, and only because its name collided with the peer's. Resolved in the next cut, the same day it was filed; the file's body is the measurement record and stays as written — its status header flips to RESOLVED with a short resolution note. **This ledger's own "In-flight" tracker was wrong in the way that matters**: its aarch64 row said "no consumer asks for an aarch64 majra binary today" while daimon ships a `daimon-aarch64` asset with this bundle inside it, and its sigil asm-offset row still described a 2.9.0 pin that cyrius 6.x's `param_load` dissolved at 2.4.5 — both retired, and the two things 2.7.3 newly leaves open (the run-under-qemu half of the CI lane; the upstream `duplicate fn 'uname_release'`) take their place, alongside the `ws_*` collision open since 2.7.1. `roadmap.md` met its first **half-shipped item**: "aarch64 cross-build wiring" was re-scoped rather than deleted, because the porting half shipped and the CI-lane half did not; its trigger is recorded as *already met*, and its old premise ("all current consumers are x86_64-server-side") was the wrong one. `state.md` had gone two toolchain patches stale (header still post-2.7.0 at 6.5.35 / sigil 3.12.9 while 2.7.1 and 2.7.2 shipped) and is rebuilt from measurement — 2.7.3, 6.6.4, sigil 3.12.18 / patra 1.14.3 / sandhi 1.9.17 / sakshi 2.5.2, 110-file snapshot, 110-hash *sorted* lock with the new `cyrius<TAB>6.6.4` trailer, 637 CI assertions, all four bundle rows re-measured, `chrono` and `sys` rows in the resolved-deps table. `testing.md` grew the most: the `qemu-aarch64` recipe the CHANGELOG and roadmap both point at, a loop-and-count-exit-139 procedure, and a `CYRIUS_SYMS` + gdbstub crash-localisation walkthrough; counts 629 → 637. `dependency-watch.md` banks the 6.6.3/6.6.4 lockfile shape and the `ESYSXLAT` contract as upgrade considerations and gains a `sys.cyr` row. `cyrius-quirks.md` earns three active entries (§9–§11): syscall numbers are x86_64 everywhere and the aarch64 backend renumbers only 44 routed rows, passing everything else through verbatim, so the failure is silent unless a name happens to collide; `struct stat` offsets are per peer; and `qemu-aarch64` runs cold, so timing windows are budgeted in seconds. `README.md` and `threat-model.md` re-anchored; `threat-model.md` also states that on aarch64 the `ipc_bind` 0600 chmod never ran from 2.6.9 through 2.7.2 (it failed closed, not open). The doc lesson is the barrier bug: `cbarrier_arrive_and_wait` had returned without arriving since 2.0.0 and SIGSEGV'd one expanded-suite run in sixteen, and two hardening passes plus a whole-tree doc sweep walked past it because the test asserted an outcome an instant error return satisfies — a green step is evidence only if the test can fail. Also corrected here: this file's `docs/audit/` paragraph still said the directory did not exist while the entry below records both 2026-08-22 reports — and the same stale premise survived in the open-questions list and forward commitment #3, both reworded; `migration-2.6.9.md` finally has a Tier 4 row; the file count at the top is re-anchored (26, not ~19). Prior entry:) | > **Last refresh**: 2026-08-22 (**whole-tree doc sweep** — every markdown file fact-checked against the 2.7.0 tree by five parallel readers, 91 findings: 25 WRONG, 38 stale, 19 missing, 9 nits. The worst was not a stale number: **README.md's Quick Start snippet did not compile** — 17 undefined functions, and under cyrius an undefined fn lowers to a trapping `ud2`, so a reader who reached for `--allow-undef` would get a SIGILL rather than a build error. It is now verified end-to-end (0 undefined, rc=0) and re-checked as part of this sweep. Three README claims were capabilities majra does not have: relay "request-response" (only ever existed in the Rust ancestor — the struct still reserves a `pending_requests` slot nothing reads), ws "pub/sub fan-out" (the module header itself says there is no bridge), and `lib/majra_majra.cyr` as the consume path (verified empirically: it is `lib/majra.cyr`). SECURITY.md was the most consequential: it told consumers PostgreSQL queries were string-interpolated and they must sanitize — fixed at 2.6.10 — while omitting the single biggest limitation majra ships, that the PG client is plaintext with cleartext-password auth. Both corrected, plus a supported-versions table still naming 2.0.x. CONTRIBUTING.md told contributors to hand-emit byte 13 (the toolchain archive says explicitly not to), quoted the cc3-era 8192 fixup cap twice instead of 16384, and omitted both CI hard gates — so a contributor following its "Adding a New Module" checklist got a red build. `cyrius-quirks.md` §8 still claimed `fl_alloc` is not thread-safe; verified against `lib/freelist.cyr` that cyrius 6.5.19 added `_fl_lock`, so it moved to the resolved archive and the one code comment citing "§8" was repointed. This file itself asserted "No `docs/development/issues/` directory" while two issues live there — the open one has been reconfirmed open at 2.7.0 and given the status header its archived sibling models. The two frozen consumer guides were handing broken recipes — an unchecked `namespace_new` (now returns 0), an unchecked `mq_dequeue` (returns 0 at the concurrency cap the example itself sets), a "distributed rate limiting via Redis" caption for a limiter that is purely in-process, and `pg_connect` under a "Distributed Backends" heading. They keep their freeze but gained a banner and inline ⚠ notes: frozen means "not current guidance", not "safe to copy". Prior entry:) | > **Last refresh**: 2026-08-22 (2.7.0 — the four additive APIs both hardening passes deferred. Doc-relevant because two of them are decisions rather than code: parallel DAG tiers were **measured before building** (thread spawn 89.4us vs a 0.184us step — a 486x ratio), and the measurement is recorded at the call site with a step-duration table so a consumer can decide from its own latency rather than re-deriving it; and pubsub's lag policy documents WHY BLOCK stays the default instead of silently changing it. `roadmap.md` deleted its 2.7.0 section on ship for the second time — the convention is holding. `state.md` refreshed and three blocker rows retired. `CHANGELOG.md` carries the tombstone-vs-remove rationale for unsubscribe, which is the non-obvious part of that design and would otherwise be rediscovered as a bug. `migration-2.6.9.md` was NOT extended: 2.7.0 changed no existing signature, so there is nothing for an existing consumer to migrate. Prior entry:) | > **Last refresh**: 2026-08-22 (2.6.10 — **second P(-1) pass**. 68 confirmed findings, 50 of them regressions 2.6.9 introduced while repairing the first audit. **New**: `docs/audit/2026-08-22-audit-pass2.md` (method, the critical, the three that deserve naming, what pass 1 missed and why it was a lens gap rather than a hard problem, the tautological-test findings, the 10 refutations with reasoning) and `docs/guides/migration-2.6.9.md` — the consumer guide for both hardening releases, covering three signature changes, three behavioural changes (the guide's own count — this entry said four until 2026-09-14), the `.patra` format change, and the full teardown-function table. It leads with "if you are on 2.6.8 or earlier, go straight to 2.6.10", because 2.6.9 alone shipped a rate-limiter bypass and a `.patra` upgrade that empties every payload. `roadmap.md` exercised its own new rule for the first time: the 2.6.10 section was DELETED on ship rather than converted into a shipped entry. `state.md` refreshed. Doc lesson recorded in the audit: 2.6.9 corrected several comments to match code, and 2.6.10 found the reverse failure — a header (`ipc.cyr`) that had been updated to promise a guarantee the code did not deliver, which is worse than the documented gap it replaced. Prior entry:) | > **Last refresh**: 2026-08-22 (2.6.9 — **first P(-1) hardening pass**. 115 confirmed findings across 23 modules, repaired in eight reviewable commits. Doc impact is unusually large because a recurring audit theme was *documentation describing code that does not exist*: `dag.cyr` claimed thread_create/join parallel tier execution and calls neither, `ws.cyr` described a pub/sub bridge with no serve loop, `admin.cyr` documented `/ratelimit/:key` while returning global counters, `majra_admin_serve` documented a localhost-only bind it did not perform, and `transport_send`/`_recv` documented a 3-arg vtable contract while forwarding 2. All corrected against the code rather than the other way round. **New file**: `docs/audit/2026-08-22-audit.md` — method, the two criticals, themes, external research (two 2026 WebSocket CVEs, NIST SP 800-38D Rev.1, CVE-2026-33895), the full 115-row finding table, the refuted pair with reasoning, and what was deliberately NOT fixed. `semver.md` gained nothing this pass but is now load-bearing for three breaking signature changes recorded in the CHANGELOG. `state.md` and `roadmap.md` refreshed; four new open-by-design entries added to state.md's blocker table (pubsub unsubscribe, PG SCRAM+TLS, per-key ratelimit stats, parallel DAG tiers) so the deferrals are tracked rather than forgotten. Prior entry:) | > **Last refresh**: 2026-08-22 (2.6.8 — toolchain + dependency-shape release. sigil moved off `[deps.sigil]` into `[deps].stdlib`, which is a **doc-heavy change**: sigil had been described as majra's one external dep in six files, and every one of them was wrong the moment it became a stdlib fold. Touched `CHANGELOG.md`, `README.md`, `CLAUDE.md`, `cyrius.cyml` (its inline rationale is load-bearing doc), `state.md`, `dependency-watch.md`, `cyrius-quirks.md`, `threat-model.md`, `roadmap.md`, `guides/testing.md`. **`state.md` was four releases stale** — last refreshed at 2.5.3 while the tree was at 2.6.7, so the whole 2.6.x arc was missing from it; rebuilt from measurement rather than patched (test counts 321 → 410, snapshot 99 → 108, bundle sizes re-measured, 2.6.0–2.6.8 release rows added). `dependency-watch.md` gained a single stated rule at the top — *never declare a git dep for a module the toolchain already folds in* — with both failure modes (silent downgrade; dropped `.deps` sidecar), since that rule is now the reason for two separate historical sections. `README.md` + `threat-model.md` carry a **consumer-facing** warning: bundles at 2.6.7 and earlier omit `sigil` from the crypto sidecars, and that failure is a runtime SIGILL, not a build error. `testing.md`'s counts were stale in both directions (expanded 112 → 200, backends 42 → 43, total 357 → 446) and now flag the live suite as CI-only. **The same release also absorbed the `majra_base64_*` collision rename** (merged in from a parallel worktree): `src/ipc_encrypted.cyr`'s `base64_encode` / `base64_decode` collided with `lib/bayan.cyr`'s same-named wrappers with *disagreeing return contracts*, so `last definition wins` made those two symbols mean whatever the consumer's include order decided. `semver.md` left the evergreen bucket to carry the new **Documented exceptions** section and its per-rename table. Prior entry:)
> **Scope**: This repo only (`majra`) — root-level files (README, CHANGELOG, CLAUDE.md, etc.) plus the entire `docs/` tree, plus `tests/soak/README.md`. Cross-repo dep/pin drift lives in [`development/dependency-watch.md`](development/dependency-watch.md), not here.

This is a **ledger**, not a one-time audit. Rewrite-in-place as docs change. Majra is the distributed-queue + multiplex engine for the AGNOS first-party tree (daimon, AgnosAI, hoosh, sutra, stiva, ifran, secureyeoman); stale module / SQL / crypto docs propagate downstream as consumer-side mis-integrations, so doc currency carries weight. The doc surface is small (26 files at 2026-09-14) but most are load-bearing.

Pattern lifted from the agnosys ledger ([`agnosys/docs/doc-health.md`](https://github.com/MacCracken/agnosys/blob/main/docs/doc-health.md)) — same buckets, majra-shaped tiers (no ADRs yet; no audit/review cadence yet; benchmarks live as frozen point-in-time artifacts).

---

## At a glance — 2026-05-10 inventory (file count re-anchored 2026-09-14)

**26 markdown files** total at 2026-09-14 (6 root + 19 under `docs/` + 1 under `tests/soak/`; the 2026-05-10 inventory counted ~19, before `docs/audit/`, `docs/development/issues/` and `migration-2.6.9.md` existed). The bucket table below is still the 2026-05-10 cut — after the 2.4.2 + 2.4.3 ship arc + the CLAUDE audit restructure — and its frozen cell has since drifted past the ~2 threshold in the refresh procedure; the tier tables are authoritative until it is re-bucketed:

| Bucket | Count | What it means |
|---|---|---|
| ✅ **Fresh — touched in 2.4.2 → 2.4.3 cycle, the 2026-05-10 sweep, or the CLAUDE audit restructure** | 10 | `CHANGELOG.md`, `docs/development/roadmap.md`, `VERSION` (ship arc); `README.md`, `docs/development/dependency-watch.md`, `docs/guides/testing.md`, `docs/development/threat-model.md` (sweep); `CLAUDE.md` (rewritten per standards), `docs/development/state.md` (new — live state ledger), `docs/development/cyrius-quirks.md` (new — toolchain gotchas, formerly inlined in CLAUDE.md). All carry the cyrius-6.1.24 / sigil-3.7.8 / agnosys-1.3.2 reality (the 2.4.5 cyrius 6.x migration on 2026-06-10 — major toolchain bump, sigil unpinned to latest, new `lib sync` + `--no-deps` workflow, `http_*`→`sandhi_server_*` and `ct_eq`→`ct_eq_bytes_lens` source ports). |
| 🟡 **Stale — refresh in place** | 0 | All 3 stale rows from the initial audit closed in the 2026-05-10 sweep — see the "Sweep" block below. |
| 🟠 **Read-through outstanding** | 0 | `docs/development/threat-model.md` read-through completed 2026-05-10: all four trust-boundary claims (admin localhost-only + read-only contract, signed_envelope `ct_eq` constant-time pk compare, ipc_encrypted nonce limit + rekey warning, patra_queue payload-injection contract) verified against current src/ — no drift. Two minor wording fixes landed (sigil "vendored" → "resolved by `cyrius deps`" in the Crypto trust boundary + Supply Chain sections). |
| 🔵 **Probably evergreen** | 4 | `SECURITY.md`, `CODE_OF_CONDUCT.md`, `LICENSE`, `CONTRIBUTING.md`. No version-tied claims that drift between minor releases (CONTRIBUTING.md's one toolchain-tied figure, the fixup-table cap, tracks the cyrius pin — corrected to 1,048,576 at 2.7.3). Re-read pass annually. `docs/development/semver.md` **left this bucket at 2.6.8** — it now carries a per-rename exceptions table, which *is* version-tied and must be appended to on every future collision rename. |
| 📦 **Archive / frozen by design** | 4 | `docs/benchmarks/results-v2.0.0.md` + `docs/benchmarks/rust-vs-cyrius-v2.0.0.md` (point-in-time at the 2.0.0 Cyrius-port cutover; the rust-vs-cyrius file is the HEADLINER — don't refresh in place); `docs/guides/migration-ifran.md` + `docs/guides/migration-secureyeoman.md` (consumer-specific migration recipes — frozen at the consumer's adoption point, not refreshed when majra moves). |
| ❓ **Open strategic question** | 0 | See [Open questions](#open-strategic-questions) for what would re-open. |

**Doc sweep completed 2026-05-10:**
- ✅ `README.md` L217 — cyrius 5.4.17 → 5.10.34 in the Rust→Cyrius comparison table.
- ✅ `docs/development/dependency-watch.md` — rewrote five items: (a) admin-profile row notes sandhi-from-stdlib path instead of `lib/http_server.cyr`; (b) stdlib modules table adds `tls.cyr` (transitive for sandhi's TLS-early-data references), drops the dated 5.4.x version notes on `hashmap.cyr` / `syscalls.cyr`; (c) `http_server.cyr` row replaced with `sandhi.cyr`; (d) `patra.cyr` row updated to 1.9.3 + the 2.4.3 SQL-surface migration note; (e) sigil "Why pinned" rewritten against the actual 2026-05-10 bisect (2.9.0 = pass, 2.9.1–3.0.1 = ed25519-NI SIGILL, 3.1.0 = aes-gcm-NI too) with a cross-link to the upstream sigil-side P1 issue; (f) "Upgrade considerations" rewritten with `cyrius deps` flow, the new sigil-upgrade gate (asm-stable NI dispatch surface), the patra/sandhi stdlib-fold story.
- ✅ `docs/guides/testing.md` — fixup-cap note rephrased to drop the dated "5.4.x" framing while keeping the 16384 number; reads as cc5-line-wide instead of stuck on the 2.4.0-era pin.
- ✅ `docs/development/threat-model.md` — read-through against current src/; all four trust-boundary claims verified; two wording fixes (sigil "vendored" → "resolved by `cyrius deps`").

**CLAUDE audit restructure completed 2026-05-10** (separate pass from the sweep — addresses standards violations that the sweep left in place):
- ✅ `CLAUDE.md` rewritten per [`first-party-documentation.md § CLAUDE.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/planning/first-party-documentation.md#claudemd) — 243 lines → 161. Removed all inlined volatile state (test counts, dep versions, consumer list, bundle counts, "currently 5.10.34"). Added required sections: Goal, Current State pointer, P(-1) Hardening (replacing the scaffolding section since majra is past scaffolding), Docs pointer. Quick Start trimmed from ~15 commands to 5. Compiler quirks moved out (see below); allocator-discipline + struct-layout conventions kept as the "Cyrius Conventions" section.
- ✅ `docs/development/state.md` scaffolded — live volatile state ledger (version, cyrius pin, resolved dep versions, test counts per suite, bundle sizes, consumer table, recent releases, in-flight blockers). Cadence: refresh every release.
- ✅ `docs/development/cyrius-quirks.md` scaffolded — toolchain gotchas (clobbering, fixup cap, hashmap-key-type pairing, `var buf[N]` static-not-stack, inline-asm fragility) + a "resolved in cc5" historical footer. Lives under `docs/development/` because *that's where toolchain-affects-how-we-work content belongs* — not under `docs/architecture/` (which is for majra design notes, not language-side gotchas). Strikethrough-and-archive resolved entries when the cyrius pin moves; don't delete.

---

**Doc cleanup completed 2026-05-10 across the 2.4.2 / 2.4.3 ship arc** (separate from the sweep + CLAUDE audit blocks above; this block records what the ship arc itself touched):
- ✅ `CHANGELOG.md` — 2.4.2 + 2.4.3 stanzas. 2.4.2 captures the cyrius 5.10.34 jump + sandhi-from-stdlib + sigil pin rationale + CI installer fix. 2.4.3 captures the patra_queue server-side SQL migration.
- ✅ `CLAUDE.md` — cyrius pin reference + sigil tag + lib/ resolution model + cc5 quirks list trimmed for the 5.10.x floor. sandhi-from-stdlib path documented.
- ✅ `docs/development/roadmap.md` — 2.4.2 + 2.4.3 marked "Recently shipped"; the QUIC/AES-NI entry rewritten against the actual sigil-asm-offset bisect; agnosys SYS_OPEN portability filed under "Waiting on upstream"; the patra-1.1.1 workaround item retired (shipped in 2.4.3).
- ✅ A paired P1 issue + roadmap entry filed in **sigil** (not in this repo) capturing the inline-asm `[rbp-N]` offset drift, with the full bisect table majra's 2.4.2 work surfaced.

---

## Tier 1 — Root files

| File | Last touched | Status | Notes |
|---|---|---|---|
| `README.md` | 2026-09-21 | ✅ Fresh | 2.9.1: the comparison table's toolchain cell 6.6.4 → 6.6.6; nothing else in the file names the pin. 2.7.3: the lede names both targets (x86_64 natively, aarch64-Linux via `cyrius build --aarch64` — verified under `qemu-aarch64`; CI cross-builds the suites for aarch64 but does not yet run them there); module-map self-label `v2.7.0, ~8,100 lines` → `v2.7.3, ~7,500 lines across 22 modules` with a pointer at `state.md` for the live figure (the 22 modules measure 7,532 lines; `src/` is 8,212 across 23 files once `main.cyr` is counted); test-matrix comment 629 → 637 plus a one-line aarch64 recipe pointing at `testing.md`; the profile table now points at `state.md` for the per-profile leaf counts instead of inlining them and notes `sys` joining the core sidecar (`chrono`, there since 2.7.1, is now a direct leaf); a new **hand-included consumers** note: a consumer including a bundle under `--no-deps` must add `lib/chrono.cyr` after `lib/syscalls.cyr`, since `time_now_ns` / `time_epoch_ns` and the internal sleep delegate to it; Rust→Cyrius table 8,212 lines / cyrius 6.6.4. The Quick Start snippet is unaffected (it reaches `error` / `counter` / `pubsub` only — none touch envelope or dag) and its 2026-08-22 compile verification stands. 2.6.8: sigil reframed as a *folded stdlib module* (not a git dep) in the lede, the profile table, and the toolchain-floor note; Rust→Cyrius table moved to `cyrius 6.5.35` / `Cyrius v2.6.x` / ~5,800 lines / "0 deps". Added a **consumer-facing sidecar warning** — bundles published at 2.6.7 and earlier omit `sigil` from the `signed`/`backends` `.deps` sidecars, which is a runtime SIGILL rather than a build failure. |
| `CHANGELOG.md` | 2026-09-21 | ✅ Fresh | 2.9.1 stanza (toolchain patch 6.6.4 → 6.6.6): what moved in the snapshot and what did not, which 6.6.5/6.6.6 fixes reach majra (each checked against the tree), the `ESYSXLAT` 44 → 60 re-derivation and the four re-tensed `sendto` comments, the agnos blocker closed upstream, footprint, a Known-issues block (the `uname_release` duplicate carried forward; sigil's frame-budget line, `note:` → `warning:` at 6.6.5; two pre-existing dev-box observations — the lint false positive on `"XXXX-XXXXX"` and the live suite's SIGSEGV with no Redis — stated as pre-existing), and a Docs paragraph. Source of truth for shipped work. Entries through 2.8.0 (the `MAJRA_ERR_*` / `majra_ws_*` namespace minor); 2.7.3 (cyrius 6.6.2 → 6.6.4, the aarch64 raw-syscall filing, the `cbarrier_arrive_and_wait` fix, the ratelimit-window change). Verified at this refresh: the version-gate heading matches `VERSION`; the issue-file link resolves (top-level `issues/`, not `archived/` — repoint it if the file moves); three Known issues, one of them upstream, and all three mirrored in `state.md`'s blocker table; the mutation-verification of every added test is recorded in the stanza rather than left for a reader to re-derive; the agnos `time_now_ns` behaviour change is stated under the fix rather than hidden. 2.6.8 recorded sigil `[deps.sigil]` → `[deps].stdlib` closing the sidecar hole and the deferred `base64_*`/bayan collision under "Known". |
| `CLAUDE.md` | 2026-08-22 | ✅ Fresh | 2.6.8: the last version-tied string ("99 files under 6.4.83") was **removed rather than updated** — the snapshot count is volatile state and belongs in `state.md`, so the quick-start line now describes only *why* `--full` is load-bearing. The `cyrius deps` comment and the 6.x workflow note were rewritten (majra declares no git deps), and the "no deps beyond toolchain + sigil" rule became "no deps beyond the toolchain" plus an explicit *never declare a git dep on a folded module* clause. Rewritten 2026-05-10 against [`first-party-documentation § CLAUDE.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/planning/first-party-documentation.md#claudemd) — durable rules only. Volatile state moved to [`docs/development/state.md`](development/state.md); compiler quirks moved to [`docs/development/cyrius-quirks.md`](development/cyrius-quirks.md). Now has all 8 required sections (identity / goal / state-pointer / quick-start / principles / rules / process-with-P(-1) / docs-pointer). 181 lines at 2.7.3 (the 2026-05-10 rewrite landed at 161, down from 243; this row said "~155" until 2026-09-14). |
| `CONTRIBUTING.md` | 2026-09-21 | 🔵 Evergreen | 2.9.1: its fixup-table figure re-worded at two of the three places to match §2's restatement — an initial capacity of 1,048,576 that grows on demand since cyrius 6.2.0, not a cap (the cc5-era 16384 history stays). Generic contributor workflow + tone guidance. Re-read annually. **Carries one toolchain-tied figure**: the fixup-table cap, quoted at three places (the module checklist, the patra-queue split note, and the "Fixup table cap" bullet). Those said 16384 — the cc5 5.x cap (8192 was cc3) — through 2.7.2; 2.7.3 corrected all three to 1,048,576, the figure the pinned 6.6.4 `cycc` reports (`fixup table full (1048576)`; so do 6.6.0 and 6.6.2), in the same round as `cyrius-quirks.md` §2 (its source, which CONTRIBUTING cites) and `state.md`'s patra-queue row (`testing.md` was re-anchored too). The figure moves only with the cyrius pin, so re-check it on a toolchain bump. The 2026-08-22 sweep corrected it (byte-13 hand-emit, the 8192 → 16384 fixup cap — right for the cc5 line — and the two missing CI gates; see the header entry); this row had kept its 2026-04-09 date through that. |
| `SECURITY.md` | 2026-09-14 | 🔵 Evergreen | Reporting policy + scope; re-read annually. **It did carry one version-tied cell**: the supported-versions table's `current: 2.7.0`, written by the 2026-08-22 sweep (which also fixed the PG string-interpolation claim and added the plaintext-PG limitation), was three releases stale by 2.7.3 — 2.7.3 made it `current: see VERSION` so the file is version-free again and stays in this bucket. If a future edit re-inlines a number there, that cell becomes a per-release touch point. |
| `CODE_OF_CONDUCT.md` | 2026-08-22 | 🔵 Evergreen | Standard. (Two lines touched by the 2026-08-22 sweep.) |
| `VERSION` | 2026-09-21 | ✅ Fresh | `2.9.1` — single source of truth, read into `cyrius.cyml` via `${file:VERSION}`. (This row had read `2.7.3` through 2.8.0 → 2.9.0 as well — corrected at 2.9.1; the earlier note stands — this row had sat at `2.5.3` through fourteen releases, 2.6.0 → 2.7.2 — the file moves every cut, so its row is the one most likely to be skipped; check it whenever the CHANGELOG row is touched.) |
| `LICENSE` | (initial commit) | 🔵 Evergreen | GPL-3.0-only. |

---

## Tier 2 — Architecture (`docs/architecture/`)

| File | Last touched | Status | Notes |
|---|---|---|---|
| `overview.md` | 2026-09-14 | ✅ Fresh | Self-label re-anchored at 2.7.3: "majra (v2.7.3, ~7,500 lines across 22 modules — live figures in docs/development/state.md)" — the 22 modules measure 7,532 lines; the whole of `src/` is 8,212 across 23 files once `main.cyr` (the core test harness) is counted, which is the figure the label had been quoting as "~8,100 lines across 22 modules" since the 2026-08-22 sweep. Module map still matches src/; 2.7.3 added no module (the new `_majra_sleep_ns` lives in `envelope`). 2.7.3 also corrected more than the label. Bundle membership is unchanged, but the distribution table's backends row had counted `patra_queue` among "5 network modules"; it is durable and file-backed, so the row now reads "4 network modules … + patra_queue", and a "Needs sigil?" footnote explains that every `.deps` sidecar names sigil. The Networking header now marks `ipc_encrypted` and `ws` as `[lib.backends]`-only; the relay globals-removal date is corrected from 2.6.1 to 2.6.0; the WsBridge arrow (a pub/sub → WebSocket loop majra does not ship) is replaced by a consumer accept loop → `ws_send_text`, with a note that `ws` ships framing only; the two Data Flow latencies are re-measured at 2.7.3; a note on the abbreviated box labels was added; and the Consumers table moved to `state.md` § Consumers, leaving a pointer. This row had been stuck at its 2026-04-19 "v2.4.x, ~5,500 lines" text while the file itself was moved to v2.7.0 by the 2026-08-22 sweep — the row lagged the doc, not the other way round. |

`docs/architecture/` holds majra design notes, not toolchain gotchas. The 2026-05-10 CLAUDE audit restructure initially mis-placed compiler quirks here; corrected and moved to [`docs/development/cyrius-quirks.md`](development/cyrius-quirks.md) per the convention that toolchain-affects-how-we-work content belongs under `development/`. No numbered architecture notes earned yet for majra design; open the series when a load-bearing invariant earns one.

---

## Tier 3 — Development (`docs/development/`)

| File | Last touched | Status | Notes |
|---|---|---|---|
| `state.md` | 2026-09-21 | ✅ Fresh | 2.9.1: rebuilt from measurement for the 6.6.6 pin — 111-file snapshot (104 + 7 unicode; `alloc_cx.cyr` new, 27 core-stdlib files changed, no first-party fold moved), 111-hash lock + `cyrius<TAB>6.6.6` trailer, bare-`lib sync` 53, bundle rows re-measured (comment-only growth), **signed / backends sidecar counts corrected 19 / 23 → 20 / 25 (stale since 2.8.1)**, aarch64 row re-verified (100× qemu / 100× native at 2.9.1), the agnos `--agnos` blocker struck through (closed upstream by 6.6.6's `lib/io.cyr`), a second sigil warning row added beside the `uname_release` one, 2.9.1 release row. Refreshed post-2.7.3: version 2.7.3, cyrius pin 6.6.4, folded sigil 3.12.18 / patra 1.14.3 / sandhi 1.9.17 / sakshi 2.5.2, 110-file snapshot (103 top-level + 7 under `lib/unicode/` — `ls lib | wc -l` says 104 because it counts the `unicode` directory entry itself), 110-hash **sorted** lock with the `cyrius<TAB>6.6.4` trailer (and why 2.7.2's was 66 entries), 637 CI assertions (153 · 304 · 152 · 28), all four bundle rows re-measured (every bundle moved; `src/` 8,212 lines across 23 files), sidecar leaves 27 core / 19 signed / 24 admin / 23 backends with `sys` in all four and `chrono` (in all four since 2.7.1) promoted to a direct leaf, new `chrono` and `sys` rows in the resolved-deps table, an **aarch64 cross-built** row in the test-surface table (same counts; verified locally, the run-under-qemu lane unwired), daimon recorded as the first non-x86_64 consumer, release rows 2.7.1–2.7.3 added, and the blocker table's aarch64 row moved from "unblocked" to "raw-syscall class RESOLVED at 2.7.3; lane still a verification task" alongside two new rows (`duplicate fn 'uname_release'` upstream; the `ws_*` collision). **It had gone two toolchain patches stale** — header still "post-2.7.0" at 6.5.35 / sigil 3.12.9 while 2.7.1 (6.5.36) and 2.7.2 (6.6.2) shipped — which is the second time this file has lagged the tree (four releases at 2.6.8); the release post-hook it keeps asking for would close that. Cadence: refresh every release. Pair-document with `doc-health.md` (state.md = code state; doc-health.md = doc state). |
| `cyrius-quirks.md` | 2026-09-21 | ✅ Fresh | 2.9.1: header re-stamped 6.6.6; §9 re-derived against the 6.6.6 emitter with the gate's own awk pipeline — 60 routed rows (44 at 6.6.4), the sixteen new sources listed, `nanosleep` 35 and `sendto` 44 re-tensed as *routed since 6.6.5* (through 6.6.4 they were `unlinkat` / `fstatfs`), and the sendto bullet now says why the per-arch spelling stays; §2 restated — the fixup table is an initial capacity of 1,048,576 that doubles on demand (growable since cyrius 6.2.0), not a cap; §7 re-measured (snapshot 111, bare subset 53 — `alloc_cx.cyr`). **2.7.3 adds three active entries, §9–§11, all earned by the aarch64 run.** §9 — syscall numbers are spelled x86_64 everywhere and the aarch64 backend renumbers only a routed set (`ESYSXLAT`, 44 rows, a `cmp x8` chain that applies to literals and `var` globals alike) while passing every other number through *as a different, valid syscall*; almost nothing warns (the one signal is a name collision with the peer's `SYS_*`), and it records how majra spells a syscall now — per-target wrapper first, a target-neutral `var _SYS_X` only when no wrapper exists on every target *and* the number is a routed row (`src/ipc.cyr`'s five networking numbers are the whole allowed set), peer-declared names in test code, verify by *running* under `qemu-aarch64`, plus the grep belt. §10 — `struct stat` is laid out per peer (`STAT_MODE` 24 on x86_64, 16 on aarch64; a hardcoded 24 reads `st_uid` there) so use the `Stat` enum. §11 — `qemu-aarch64` runs cold (first call into a path ~1 ms under TCG), so timing windows are budgeted in whole seconds and elapsed-time assertions are floors, never ceilings. §7 also gained **"`--no-deps` prepends nothing"** — the manifest `[deps].stdlib` list is not consulted by a `--no-deps` build, so a stdlib module a `src/` file reaches must be included by every entry point that reaches it (why thirteen entry points gained `lib/chrono.cyr`), and its bare-`lib sync` count moved to 52 files at 6.6.4 (49 at 6.5.35). The 2026-08-22 sweep (2443b6a, first tagged in 2.7.1) archived §8 (`fl_alloc` thread-safety) after verifying `_fl_lock` in `lib/freelist.cyr`, but the same commit deleted the whole resolved-archive section (+8/−53; the committed file ended at §7), so §8 was left in neither place; 2.7.3 restored the archive with #8 in it. **§6 rewritten at 2.5.2** — it had gone stale describing cyrius 6.1.x's "undefined symbol = warning + runtime `ud2`" behavior; the toolchain now hard-errors on a *reachable* undefined call (no binary emitted) and warns only on unreachable ones. Verified empirically at 6.4.62 *and* 6.4.83, so this was doc rot, not a pin change. §6 also gained the driver-asymmetry note (`cyrius bench`/`audit` inject the manifest stdlib set, `build --no-deps` doesn't — a green CI doesn't imply `audit` compiles), §7 gained the snapshot-shadowing rule + `--full` corrections, §5's sigil reference caught up to 3.12.1, §4 re-verified unchanged. Scaffolded by the CLAUDE audit restructure — seven active toolchain gotchas + a "resolved in cc5" historical footer. Strikethrough-and-archive resolved entries when the cyrius pin moves; don't delete. Lives under `development/` (not `architecture/`) because it's about how the toolchain affects how the work happens, not about majra design. |
| `roadmap.md` | 2026-09-21 | ✅ Fresh | **2.9.1: the pre-written "Moving the cyrius pin to 6.6.6" section shipped and was deleted** (its measured claims — pair returns, the `#ifdef`-split `_IPC_SYS_SENDTO`, no PE flags — re-checked and recorded in the CHANGELOG first); "Waiting on upstream" is empty for the first time, with a note saying why (6.6.6's `lib/io.cyr` closed the `_agnos_getenv` gap), and the majra-side half — agnos-portable backends / patra-queue suites — is a new 2.9-line item with scope, target and trigger. **2.9.0: the 2.9.0 section shipped and was deleted, a `3.0.0` section records the one promised break (the bare `ERR_*` aliases), and the backward-facing narrative was trimmed — the aarch64, SCRAM and agnos entries now state what remains rather than what happened. "2.8 line" is now "2.9 line".** **2.8.0: both queued namespace items shipped and were deleted; the P(-1) sweep is the 2.8.1 item, and "2.7 line" is now "2.8 line".** **2.7.3: the first half-shipped item.** "aarch64 cross-build wiring" was *re-scoped rather than deleted*, because the porting half shipped (no *unrouted* raw x86_64 syscall number remains in arch-neutral code — only `src/ipc.cyr`'s `ESYSXLAT`-routed networking vars and per-arch sendto remain; every suite, fuzz harness, soak and example passes cross-built under `qemu-aarch64`, verified locally) and the CI-lane half did not. The surviving item is titled "aarch64 CI lane — build **and run** under `qemu-aarch64`", carries a PATCH-sized scope with the recipe pointer to `testing.md`, insists the lane must *run* the binaries (two of the three 2.7.3 defects emitted no build-time warning on any toolchain — the build-only CI gate 2.7.3 added, which fails on any `duplicate symbol 'SYS_` or raw-syscall diagnostic, is only the belt), and records its trigger as **already met** — daimon ships a `daimon-aarch64` asset with `dist/majra.cyr` inside it. The old item's premise, "all current consumers are x86_64-server-side", was the thing 2.7.3 disproved. The 2.7-line intro and table row also gained "or the next PATCH, where it adds no API" (which is where that item is aimed), and the Waiting-on-upstream item "agnos `--agnos` build for the non-core profiles" was re-scoped: its 2.5.0 blocker (`lib/patra.cyr` reaching `SYS_LSEEK` unguarded on agnos) is recorded as cleared — patra 1.14.3 takes it from the agnos peer and `dist/majra-backends.cyr` cross-builds `OK` under `--agnos` — with a new blocker filed in its place (`undefined function '_agnos_getenv'`, `lib/io.cyr` → `lib/args_agnos.cyr`, a module no sidecar names) and the majra-side work it had said was "none expected" now named (the backends and patra-queue suites call `sys_unlink` / `sys_stat` with the Linux arity and `SYS_SOCKETPAIR`, which has no agnos row). **Restructured at 2.6.9 to be forward-facing only.** It had grown ~100 lines of per-release "Recently shipped" sections back to 2.3.x — a shadow changelog duplicating `CHANGELOG.md`, with only ~20 lines actually about future work. Now scheduled against **release targets** (2.6.10 hardening / 2.7.0 deferred-audit APIs / 2.7 line / waiting-on-upstream / non-goals), with every entry naming the **trigger** that would move it, so promotion is a decision rather than a mood. Records why the items cannot all be patch releases — `semver.md` reserves PATCH for no-API-change work, so each added public function takes the next MINOR and "the 2.7.x line" is a development line rather than a run of patch numbers. The 2.6.9 audit deferrals (pubsub unsubscribe, PG SCRAM+TLS, per-key ratelimit stats, parallel DAG tiers) are filed here with their reasons rather than left implicit. Verified nothing still-open was buried in the deleted history. The convention is now enforced in `CLAUDE.md` (dev-loop step 7 and closeout step 4 say *delete* shipped items, where closeout step 4 previously said to *add* them). |
| `dependency-watch.md` | 2026-09-21 | ✅ Fresh | 2.9.1: lede v2.7.3 → v2.9.1; snapshot-size list gains 111 under 6.6.6 (and 110 through 6.6.5) with the 27-changed / no-fold-moved note; sigil / sakshi / patra / sandhi lines say *unchanged, latest tag* rather than a new number; the `ESYSXLAT` bullet answers its own "check on the next bump" instruction (44 → 58 → 60, the sixteen sources, why majra's per-arch sendto stays); the lockfile bullet banks 6.6.6's `cyrius deps --lock` refusal of unhashable files; the watched-upstream sigil item is re-checked (unchanged) and gains a sibling — the over-budget `var buf[262144]` warning 6.6.5 promoted from a `note:`. 2.7.3: lede v2.7.0 → v2.7.3; sigil heading 3.12.9 → **3.12.18** with a 3.12.16 → 3.12.18 bump bullet — the first fold since the move into `[deps].stdlib` where sigil reached into a stdlib module majra had never declared (`agnosys_uname` → `lib/sys.cyr`'s `sys_uname`; without the include the build reports OK and lowers the call to a trapping `ud2`), so a **`sys.cyr` row** joins the stdlib table and `"sys"` joins `[deps].stdlib`; `chrono.cyr` row rewritten as a *direct* majra dep (`time_now_ns` / `time_epoch_ns` / `_majra_sleep_ns`); patra 1.14.3, sandhi 1.9.17, sakshi 2.5.2 stamped at the 6.6.4 fold; snapshot history extended (108 under 6.5.31–6.5.36, 110 under 6.6.4); two new upgrade-consideration bullets — the **6.6.3/6.6.4 lockfile shape** (sorted; `cyrius<TAB>6.6.4` trailer; refuses a moved stdlib hash under an unchanged pin, `cyrius deps --relock` accepts) and the **aarch64 `ESYSXLAT` contract** (which rows are renumbered, why everything else passes through verbatim, and the aarch64 leg now expected on every cyrius bump); and the upstream `duplicate fn 'uname_release'` warning (sigil 3.12.18 vs `lib/sys.cyr:203`, byte-identical, harmless) filed as a watched item. Its sigil heading had lagged two pins (3.12.9 / 6.5.35 while 2.7.1 and 2.7.2 shipped 6.5.36 and 6.6.2). Refreshed at the 2.5.2 toolchain bump: sigil heading 3.11.1 → 3.12.1 + a 3.11.1→3.12.1 bump bullet; **new `sakshi = 2.4.6` section** covering why majra declares a dep it never calls (sigil's manifest pins 2.4.3 and the overlay was downgrading the snapshot's 2.4.6) and what to re-check on the next sigil bump; "Stdlib changes" consideration records the 99-file snapshot holding across 6.4.62 → 6.4.83 plus a general warning about snapshot-vs-declared-dep shadowing. Prior sigil-footprint / bisect content carries through. |
| `semver.md` | 2026-09-15 | ✅ Fresh | **2.8.0 added the two `majra_ws_*` rows plus a migration note to the renames table, and a new Deprecations section (bare `ERR_*` → `MAJRA_ERR_*`, removal at 3.0.0).** Was evergreen through 2.6.7 (promise + categories only). The 2.6.8 base64-collision rename added a **Documented exceptions** section: a symbol whose meaning was decided by consumer include order was never covered by the promise, so renaming out of a collision is not an API change and may ship in any cut — gated on (1) the definitions genuinely disagreeing and (2) the rename producing a compile error at every call site. Carries a per-rename table (both `base64_*` rows stamped 2.6.8) — append a row on every future collision rename. |
| `threat-model.md` | 2026-09-21 | ✅ Fresh | 2.9.1: the Crypto-trust-boundary line re-anchored to cyrius 6.6.6 (sigil 3.12.18 unchanged) and `cyrius.lock`'s 111 hashes. 2.7.3: the Crypto-trust-boundary line re-anchored to sigil 3.12.18 under cyrius 6.6.4 and `cyrius.lock`'s 110 hashes (was 3.12.9 / 6.5.35 / 108); the IPC access-control note gained a ⚠ that on aarch64-Linux the 0600 `fchmod` never ran from 2.6.9 through 2.7.2 and every `ipc_bind` failed **closed** rather than leaving a wider socket behind (mechanism in CHANGELOG.md § [2.7.3]; 2.7.3 routes it through the per-target `sys_fchmod`) — verified that the claim is fail-closed, which is the part that matters to this file's trust-boundary table; a duplicated pubsub lag-policy row in the attack-surface table was deleted. The four trust-boundary claims below remain verified. **2.5.2 pass closed two stale claims the 2026-05-10 read-through didn't cover**: the Crypto-trust-boundary line still cited the sigil **3.7.8** pin (four bumps behind — now 3.12.1 and framed as "tracking latest"), and Supply Chain still said "one first-party dep" (now two: sigil + the newly-declared sakshi, with a note that an inherited transitive pin isn't a pin majra controls). The four trust-boundary claims below remain verified against current `src/`. Read-through completed 2026-05-10: admin localhost-only + read-only contract (src/admin.cyr L4, L8, L124), signed_envelope `ct_eq` constant-time pk compare (src/signed_envelope.cyr L125), ipc_encrypted nonce limit at 2^31 with hard-fail at 2^32 (src/ipc_encrypted.cyr L17, L127), patra_queue payload-injection contract (still applies after the 2.4.3 server-side WHERE rewrite — payload is the only consumer-provided string concatenated into SQL) — all four match current src/. Two wording fixes landed: sigil "first-party, vendored" → "resolved into `lib/sigil.cyr` by `cyrius deps`" in the Crypto trust boundary + Supply Chain sections. |

---

## Tier 4 — Guides (`docs/guides/`)

| File | Last touched | Status | Notes |
|---|---|---|---|
| `testing.md` | 2026-09-21 | ✅ Fresh | **2.9.1: the suite table had drifted — it still said 153 / 304 / 152 / 28 = 637 (2.7.3) through 2.8.0, 2.8.1, 2.8.2 and 2.9.0 while the suites grew to 203 / 645 / 547 / 71 = 1,466** (state.md carried the right numbers throughout; this file's own note points readers there, but the table above that note is what they read). Re-anchored, dev-box total 1,466 / 1,502 with live, benchmark count 17 → 19 (`mq_lifecycle`, `mq_enqueue_4producers` since 2.8.2), fixup-cap wording restated (initial capacity, growable), `ESYSXLAT` count 44 → 60 at the 6.6.6 pin. **2.7.3 — the largest change since the file was written.** Counts re-anchored (core 150 → 153, expanded 299 → 304, CI 629 → 637, 673 with the 36 live) and a new **aarch64 cross-build (qemu-user)** section that the CHANGELOG, roadmap, README and state.md all point at: the `cyrius build --aarch64 --no-deps <entry> build/<name>_aarch64 && qemu-aarch64 ./build/<name>_aarch64` recipe for every entry point, the `ESYSXLAT` reason a build-only `--aarch64` step proves nothing, the qemu timing caveat (budget windows in whole seconds), a **loop-and-count-exit-139** procedure with the 2.7.3 baseline recorded against 2.7.2's, and a four-step **crash-localisation** walkthrough (`CYRIUS_SYMS` symbol map → qemu `-g` gdbstub → resolve `pc` against the map → read the aarch64 frame, parameters-then-locals below `x29`) that is the recipe which found `_map_find+0xf8`. The soak list also gained the three targets it had omitted (`soak_pubsub`, `soak_relay`, `soak_heartbeat`) — `tests/soak/README.md` had them, this file listed only `soak_queue`. **2.5.2**: the setup line still said `cyrius lib sync && cyrius deps` — corrected to `lib sync --full` (load-bearing since 6.4.x; the bare form omits the toolchain modules and yields a runtime SIGILL, not a build error) and reworded to cover both git deps now that sakshi is declared. Fixup-cap note rephrased in the 2026-05-10 sweep to drop the "5.4.x" framing while keeping the 16384 number — it then read as cc5-line-wide rather than stuck on a 2.4.0-era pin; 2.7.3 re-anchored both fixup-cap mentions to 1,048,576 at the 6.6.4 pin, keeping 16384 as the cc5 5.x history. |
| `migration-2.9.0.md` | 2026-09-15 | ✅ Fresh | The 2.9.0 consumer guide: both wire breaks (encrypted-IPC handshake, envelope domain prefix), the fail-closed `verify(se, 0)` return, and the additive release/unsubscribe/Origin APIs. Written with the release, not after it. |
| `migration-2.6.9.md` | 2026-08-22 | 📦 Frozen | Consumer guide for the 2.6.9 + 2.6.10 hardening releases — three signature changes, three behavioural changes, the `.patra` format change, the teardown-function table — leading with "if you are on 2.6.8 or earlier, go straight to 2.6.10". Frozen at that migration point: deliberately NOT extended at 2.7.0 (no existing signature changed) and untouched by 2.7.1–2.7.3 (none changed a signature either). Write a new `migration-<version>.md` when a later minor breaks one, rather than extending this file. Neither 2.8.0 item is such a trigger: the queued `ws_*` rename ships under `semver.md` § Documented exceptions, whose migration note lives in `semver.md` and `CHANGELOG.md`, and the `ERR_*` → `MAJRA_ERR_*` change keeps the bare names as deprecated same-value aliases. (This row was missing from 2026-08-22 until 2026-09-14 — the header's 2.6.10 entry named the file as new, but no tier row was added.) |
| `migration-ifran.md` | 2026-08-22 | 📦 Frozen | Consumer-specific recipe captured at ifran's adoption point. Don't refresh in place when majra evolves; ifran is the source of truth for what got migrated. Re-author only if ifran returns for a second migration round. The 2026-08-22 sweep added the frozen-means-not-safe-to-copy banner and inline ⚠ notes (the freeze covers the recipe, not the warnings around it); this row had kept its 2026-04-08 date through that. |
| `migration-secureyeoman.md` | 2026-08-22 | 📦 Frozen | Same posture as `migration-ifran.md`, same 2026-08-22 banner + ⚠ notes. |

---

## Tier 5 — Benchmarks (`docs/benchmarks/`)

Date / version-stamped, frozen by design. Each major perf cutover gets its own file — old files stay verbatim as the historical record. Live bench history lives in `bench-history.csv` (gitignored).

| File | Pinned at | Status | Notes |
|---|---|---|---|
| `results-v2.0.0.md` | v2.0.0 | 📦 Frozen | Snapshot of the v2.0.0 bench surface (Cyrius-port cutover). Don't refresh in place — capture the next snapshot in a new file (`results-v2.5.0.md` or similar) at the next significant perf-affecting release. Today's bench numbers from `bench-history.csv` differ from these in places (e.g. `pubsub_publish_nosub` improved); that's expected. |
| `rust-vs-cyrius-v2.0.0.md` | v2.0.0 | 📦 Frozen — HEADLINER | Rust→Cyrius port comparison at the 2.0.0 cutover. Heritage artifact, deliberately kept at `docs/benchmarks/`. |

---

## Tier 6 — Test READMEs

| File | Last touched | Status | Notes |
|---|---|---|---|
| `tests/soak/README.md` | 2026-08-22 | ✅ Fresh | Lists soak_queue / soak_pubsub / soak_relay / soak_heartbeat — all four targets ship per `tests/soak/`. No version-tied claims to drift; refresh only when targets are added/retired. (Eight lines touched by the 2026-08-22 sweep; this row had kept its 2026-04-19 date through that.) |

---

## What this repo does NOT have yet (and doesn't need to invent)

The agnosys ledger has tiers for **ADRs**, **audit reports**, **engineering reviews**, and **engineering issues**. Majra has none of those structures, by design:

- **No ADRs.** Decision velocity is low; rationale rides CHANGELOG entries + roadmap notes + design comments. The 2.3.0 manifest migration, the 2.4.0 four-profile distlib, and the 2.4.2 sandhi-from-stdlib swap were all candidates and none earned an ADR — the entries in CHANGELOG carry the reasoning. Re-evaluate at v3.0.0 cut. Open the directory only when a decision is reversible-but-load-bearing and would benefit from a referenceable "we decided X because Y" artifact.
- **`docs/audit/` exists, but as point-in-time reports, not a cadence.** Three files, each frozen at the pass that wrote them: `2026-09-15-audit.md` (2.8.1, the third P(-1) pass — 130 findings), plus `2026-08-22-audit.md` (2.6.9, the first P(-1) pass — 115 findings) and `2026-08-22-audit-pass2.md` (2.6.10 — 68 findings, 50 of them regressions 2.6.9 introduced). A new report lands when CLAUDE.md's P(-1) step 5 runs, not on a schedule, and prior reports are never refreshed in place (same posture as `docs/benchmarks/`). Between passes the fuzz harnesses (`fuzz/*.fcyr`) + soak tests carry the assurance for queue/pubsub/relay correctness — with the 2.7.3 caveat that neither of the two passes could see an arch-dependent syscall-number defect from x86_64, and neither caught a barrier test that asserted an outcome an instant error return satisfies. (This bullet said "No `docs/audit/` cadence … open the directory only if" from 2026-05-10 until 2026-09-14, while the header above had recorded both reports since 2026-08-22.)
- **No `docs/development/reviews/` cadence.** Same logic — internal review artifacts emerge if/when audit cadence does.
- **`docs/development/issues/` exists** — none open, two archived. (The hoosh-filed
`2026-06-23-ratelimit-fn-collision-namespace.md` was deleted at 2.8.0 rather than fixed
here: the collision was hoosh's to resolve, and hoosh did, renaming its own limiter to
`hoosh_ratelimit_*` on 2026-07-23. majra's bare `ratelimit_*` names stay.) One was **resolved at 2.7.3**
(`archived/2026-09-14-raw-x86-syscall-numbers-aarch64.md` — filed 2026-09-14 by a daimon consumer
from a `cyrius build --aarch64` of the vendored `dist/majra.cyr`: the first issue filed
from a cross-build of a shipped bundle (the other two were consumer-filed too: the since-deleted
ratelimit issue by hoosh, from the build-log warnings its vendored `src/vendor/majra.cyr`
produced, and the archived pq-dequeue issue by agnosai, from its benchmark), filed against 2.7.2 and resolved in the next cut the same
day. Its body is the measurement record — the `ESYSXLAT` routing table, the per-site
derivation, the qemu confirmation — and stays as written; its status line flips to
RESOLVED at 2.7.3 with a short resolution note, and it moved to `archived/` at 2.7.3 per
the pq-dequeue precedent, with every link repointed). The other is
`archived/2026-08-10-pq-dequeue-is-linear-...`, resolved at 2.6.2. majra has
no local `issues/README.md` lifecycle doc; the archived file's header shape
(`**Status:** ✅ **RESOLVED in majra <version>** (<date>)`) is the convention in practice, and the 2.7.3
file already follows it exactly (`**Status:** ✅ **RESOLVED in majra 2.7.3** (2026-09-14, pin
6.6.2 → 6.6.4)`, then a `**Resolution:**` paragraph). Upstream issues filed *by* majra live in the upstream repo's `issues/` directory (the sigil-2.9.1+ asm-offset issue we filed on 2026-05-10 sits at [`sigil/docs/development/issues/2026-05-10-cyrius-510-asm-stack-frame-drift-breaks-ni-paths.md`](https://github.com/MacCracken/sigil/blob/main/docs/development/issues/2026-05-10-cyrius-510-asm-stack-frame-drift-breaks-ni-paths.md), not in majra). Cross-references from majra's roadmap point at those upstream tickets. The directory takes consumer-filed defects against shipped bundles as well as self-internal blockers; a problem that is upstream's to fix is still filed upstream (the 2.7.3 `duplicate fn 'uname_release'` is sigil's, and lives only as a CHANGELOG Known issue, a `state.md` blocker row and a `dependency-watch.md` watch item here — no issue file).

---

## Open strategic questions

None outstanding at 2.7.3 (the anchor here had read "for the 2.4.3 cut" through twenty-two releases, 2.4.4 → 2.7.2). This section will repopulate when:

- A new doc category appears (e.g. an `adr/` if a reversible architectural decision needs a referenceable record — `docs/audit/` already opened at 2.6.9, see the bullet above).
- A consumer migration becomes generic enough that `docs/guides/migration-*.md` should be replaced by a single `docs/guides/adopting-majra.md`. Currently per-consumer is the right shape.
- A second benchmark snapshot lands and we need to decide whether `bench-history.csv` (gitignored, live data) supersedes the date-pinned files entirely.

---

## In-flight (blocked, not stale)

- **aarch64 CI lane — the run half** — the porting half shipped at 2.7.3 (no *unrouted* raw x86_64 syscall number left in arch-neutral code — only `src/ipc.cyr`'s `ESYSXLAT`-routed networking vars and per-arch sendto remain; every suite, fuzz harness, soak and example passes cross-built under `qemu-aarch64`, verified locally), and CI gained a build-only `aarch64 cross-build gate` plus a raw-syscall-literal grep gate in the same release. What remains is running the binaries under `qemu-aarch64` on the runner — a roadmap item whose trigger is already met. Doc-side everything is in place: `testing.md` § aarch64 cross-build carries the recipe and says the run half "is still a roadmap item"; `state.md`'s toolchain row, test-surface row and "aarch64 CI lane (run, not just build)" blocker row say the lane is unwired; `README.md` says so in its lede and again in the Building comment; `cyrius-quirks.md` §9 and `dependency-watch.md`'s `ESYSXLAT` bullet both call the run-under-qemu lane "still a roadmap item"; and `ci.yml`'s aarch64 cross-build gate comment says it "stays a roadmap item" and "the runner has no qemu-user" (plus "this lane is x86-only" in the raw-literal gate comment). When the run lane lands, close every one of those phrasings (`grep -rniE 'unwired|not wired|still a roadmap item|stays a roadmap item|does not yet run|x86-only|no qemu-user' README.md docs/ .github/`), leave `state.md`'s release-history row, the 2026-09-14 issue filing and the CHANGELOG as historical, and delete the roadmap item — nothing else is owed here until then.
- **`duplicate fn 'uname_release'` — `lib/sigil.cyr:746` vs `lib/sys.cyr:203`** — upstream (sigil 3.12.18 still ships a byte-identical copy of the accessor it now routes through `lib/sys.cyr`); harmless, last definition wins. *Re-checked at the 6.6.6 pin (2.9.1): unchanged — the fold is byte-identical.* Its new sibling, **sigil's over-budget `var buf[262144]` warning** (`lib/sigil.cyr:25118`; 6.6.5 promoted the compiler's `note:` for the static-storage fallback to a `warning:`, same storage as always), is recorded in the same three places — CHANGELOG 2.9.1 Known issues, a `state.md` blocker row, a `dependency-watch.md` watched-upstream bullet — and closes when sigil moves the bank to `alloc()`. Recorded as a CHANGELOG Known issue, a `state.md` blocker row and a `dependency-watch.md` watch item. Clears when a future cyrius snapshot folds a sigil that drops its copy; the only majra-side action then is deleting the three notes.
- ~~**`ws_recv_frame` / `ws_send_text` collide with `lib/ws.cyr`**~~ — **resolved at 2.8.0** (`majra_ws_send_text` / `majra_ws_recv_frame`, under semver.md's collision exception). The doc debt was paid in the same cut: both rows and a migration note in `semver.md`, the CHANGELOG stanza, and the README / overview.md / `src/ws.cyr` references. No separate migration guide, following the 2.6.8 base64 precedent.
- *Retired 2026-09-14* — both rows that had sat here since 2026-05-10 were resolved at **2.4.5** (2026-06-10) and nobody closed them: the **sigil asm-offset drift** was dissolved by cyrius 6.x's `param_load` pseudo (the 2.9.0 pin this section still described was lifted then; sigil is a 3.12.18 stdlib fold now), and the **`lib/agnosys.cyr:791` SYS_OPEN** blocker went away with agnosys 1.3.2 at the same cut, then became moot outright when sigil 3.8.1 dropped agnosys from the graph (2.5.1). The second row's premise — "no consumer asks for an aarch64 majra binary today" — was the exact premise 2.7.3 disproved: daimon had been shipping a `daimon-aarch64` asset with `dist/majra.cyr` inside it, and its cross-build is what surfaced the three raw-syscall defects.

---

## Forward doc-policy commitments

| # | Commitment | Trigger | Source | Notes |
|---|---|---|---|---|
| 1 | **Bench snapshot retention** — keep `docs/benchmarks/results-v*.md` + `rust-vs-cyrius-v*.md` verbatim through at least v3.0.0; re-evaluate at the major cut whether pre-2.0 snapshots get folded into a single historical summary. | v3.0.0 cut | This file | Today the surface is 2 files (the v2.0.0 cutover pair) — purge pressure is zero. |
| 2 | **Migration guide retention** — per-consumer recipes stay frozen at the consumer's adoption point. If a consumer returns for a second migration round, write a second file (`migration-<consumer>-v2.md`) rather than refreshing the original in place. | When the next consumer migrates | This file | Pattern keeps the historical record clean. |
| 3 | **Open audit/review tiers only on a real trigger.** `docs/audit/` opened on the 2.6.9 P(-1) trigger (two point-in-time reports, both frozen — see the bullet above); don't add a `docs/development/reviews/` directory without an equivalent forcing function, or it degrades into checklist noise. | A new `docs/audit/` report lands when CLAUDE.md's P(-1) step 5 runs; `reviews/` has no trigger yet | This file | Agnosys earned its audit cadence from kernel-syscall-boundary work; majra's audit reports came from the first two P(-1) passes, as point-in-time artifacts rather than a cadence. (This row said "don't add empty `docs/audit/`" from 2026-05-10 until 2026-09-14, three weeks after the directory had two reports in it.) |

---

## Refresh procedure

When docs are touched:

1. Find the affected row in the relevant tier table.
2. Update **Last touched** column to the new date.
3. Update **Status** column if the bucket changed.
4. Update **Notes** column if the next step changed.
5. If a doc moved or was archived, update its row to reflect the new home.
6. Re-anchor "Last refresh" date in the header.

When the bucket counts at the top drift by more than ~2 in any cell, refresh the at-a-glance table.

This file is touched on **every release's doc-sync step** — `CLAUDE.md` dev-loop step 7 (row touch), closeout step 4 and P(-1) step 8 (sweep) — and **opportunistically** in between, whenever another doc is touched. It is not periodic. The 2.4.2 → 2.4.3 ship arc established the baseline; the header above was refreshed at every cut from 2.6.8 through 2.7.0 (plus the post-2.7.0 whole-tree sweep), skipped at 2.7.1 and 2.7.2, and refreshed again at 2.7.3. (This sentence said "not tied to releases" from 2026-05-10 until 2026-09-14, contradicting the process in `CLAUDE.md` and the header's own record.)

---

## What this file is NOT

- Not a substitute for [`development/dependency-watch.md`](development/dependency-watch.md) (which holds live cyrius / sigil / patra / sandhi version-tracking state).
- Not a CHANGELOG (which records what shipped, not what's stale).
- Not a roadmap (forward work lives in [`development/roadmap.md`](development/roadmap.md)).
- Not a per-doc review log (we record the result of an audit pass, not the per-doc reasoning).

---

*Last refresh: 2026-09-14 (2.7.3 cyrius 6.6.2 → 6.6.4 + the aarch64 raw-syscall filing + the barrier fix — CHANGELOG / VERSION / README / overview.md / state.md / cyrius-quirks.md / roadmap.md / dependency-watch.md / threat-model.md / testing.md / SECURITY.md / CONTRIBUTING.md rows refreshed; `migration-2.6.9.md` row added; issues paragraph gained the 2026-09-14 filing; the `docs/audit/` paragraph, open-questions list, forward commitment #3, the file count, the In-flight tracker, the refresh-cadence paragraph ("not tied to releases" → every release's doc-sync step), the CLAUDE.md row's line count (~155 → 181; date unchanged), and the CODE_OF_CONDUCT.md / migration-ifran.md / migration-secureyeoman.md / tests/soak/README.md rows — re-dated to the 2026-08-22 sweep that last touched those files — corrected (CONTRIBUTING.md was re-dated the same way, then refreshed again at 2.7.3 when its fixup cap was corrected 16384 → 1,048,576). This line had sat at 2026-06-15 while the header above was refreshed seven times, 2.5.1 through the 2.7.0 whole-tree sweep — the header is authoritative.) Refresh in place when docs are touched.*
